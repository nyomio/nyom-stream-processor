apiVersion: v1
kind: Service
metadata:
  labels:
    app: logstash
  name: logging-logstash
  namespace: {{ .Values.global.namespace }}
spec:
  ports:
    - port: 4560
      targetPort: 4560
      name: logstash-api
  selector:
    app: logstash
