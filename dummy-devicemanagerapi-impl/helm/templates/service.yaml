apiVersion: v1
kind: Service
metadata:
  labels:
    app: dummy-devicemanagerapi-impl
  name: {{ .Release.Name }}-devicemanagerapi-impl
  namespace: {{ .Values.global.namespace }}
spec:
  selector:
    app: dummy-devicemanagerapi-impl
  ports:
    - port: 9500
      targetPort: 9500
